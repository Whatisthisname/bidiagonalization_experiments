\documentclass[a4paper,12pt]{article}

\usepackage{a4wide}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{mathtools}
\usepackage{amssymb}
\usepackage{amsthm}
\theoremstyle{definition}
\usepackage{physics}
\usepackage{bbm}
\usepackage{graphicx}
\usepackage{amsmath}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator*{\argmin}{arg\,min}
\usepackage{hyperref}
\usepackage{graphicx}  % For including images
\usepackage{xcolor}  % For a colorfull presentation
\usepackage{listings}  % For presenting code 
\usepackage{float}
\usepackage{multicol}


% Definition of a style for code, matter of taste
\lstdefinestyle{mystyle}{
  language=Python,
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color[HTML]{F7F7F7},
  rulecolor=\color[HTML]{EEEEEE},
  identifierstyle=\color[HTML]{24292E},
  emphstyle=\color[HTML]{005CC5},
  keywordstyle=\color[HTML]{D73A49},
  commentstyle=\color[HTML]{6A737D},
  stringstyle=\color[HTML]{032F62},
  emph={@property,self,range,True,False},
  morekeywords={super,with,as,lambda},
  literate=%
    {+}{{{\color[HTML]{D73A49}+}}}1
    {-}{{{\color[HTML]{D73A49}-}}}1
    {*}{{{\color[HTML]{D73A49}*}}}1
    {/}{{{\color[HTML]{D73A49}/}}}1
    {=}{{{\color[HTML]{D73A49}=}}}1
    {/=}{{{\color[HTML]{D73A49}=}}}1,
  breakatwhitespace=false,
  breaklines=true,
  captionpos=b,
  keepspaces=true,
  numbers=none,
  showspaces=false,
  showstringspaces=false,
  showtabs=false,
  tabsize=4,
  frame=single,
}
\lstset{style=mystyle}

\begin{document}
\title{Numerically stable gradients of Golub-Kahan Bidiagonalization}
\author{Theo Würtzen and Nicholas Krämer}
\maketitle
\tableofcontents
\newpage

\section{Rambling:}

Bidiagonalization suffers from loss of orthogonality of the vectors in $L$ and $R$. This can be solved by reorthonormalizing the new vector against all computed vectors.

Ideally, that should've been enough, but it can be shown that it is not - we need at least 3 applications of reorthonormalization for this to work. \footnote{Show this in an experiment}.

In the adjoint system we can do something similar.

UPARROW IS PHI, DOWNARROW IS XI

\section{Background}
While Bidiagonalization has well-understood forward algorithms and constraints, computing its gradient in a numerically stable way is challenging (TODO show that it is unstable from example). This paper builds from a connection between Bidiagonalization and Hessenberg factorization, namely that any Bidiagonalization can be computed by performing Hessenberg factorization on an appropriately constructed symmetric matrix, allowing us to then leverage the stable gradient of Hessenberg factorization proposed in (TODO cite Nico).

\subsection{Notation}
Let $e_j$ be the $j$th unit vector. Denote by ``$\circ$'' the element-wise 
matrix product which binds weaker than the standard matrix product and stronger than addition, and define the matrices
\begin{align*}
\mathbf{1} := [1]_{i,j=1}^K \qquad
I_{\leq} := [\delta_{i \leq j}]_{i,j=1}^K \qquad
I_{<} := [\delta_{i < j}]_{i,j=1}^K \qquad
I_{\ll} := [\delta_{i+1 < j}]_{i,j=1}^K
\end{align*}
so that for example, $\mathbf{1} \circ A = A$, $I_{\leq} \circ A$ extracts the lower triangular matrix 
of $A$ (including the diagonal), and $I_{\ll} \circ A = 0$ enforces Hessenberg form.

\subsection{Matrix free linear algebra}
Matrix-free linear algebra refers to implementing matrix operations without explicitly forming or storing the full matrices. Instead, we work with functions that compute matrix-vector products. This is particularly useful when dealing with large matrices where storing them would be impractical, or when the matrix is defined implicitly through some operation, where we never need the full matrix, only specific matrix-vector products.
For a linear operator $A: \mathbb{R}^m \rightarrow \mathbb{R}^n$, instead of storing an $n \times m$ matrix, we represent it as a function:

$$\text{matvec}: \mathbb{R}^m \times \Theta \rightarrow \mathbb{R}^n$$
such that $\text{matvec}(v, \theta) = A_\theta v$ for any vector $v \in \mathbb{R}^m$ and parameters $\theta \in \Theta$. This approach enables a general way to handle general linear operators, such as matrix-vector products with sparse and structured matrices, the jacobian-vector product of a neural network, finite-difference operators, discrete fourier transforms and other linear operators acting between vector spaces.

\subsection{Bidiagonalization}
$$\texttt{Bidiagonalize}_K: (A, v) \mapsto (L, R, B, r, c)$$
Bidiagonalizing an arbitrary matrix $A \in \mathbb{R}^{N \times M}$ with initial vector $v \in \mathbb{R}^M$ and a prescribed number of iterations $K \in \mathbb{N}_+$, will yield column-orthonormal $L \in \mathbb{R}^{N \times K}$ and $R \in \mathbb{R}^{M \times K}$, bidiagonal $B \in \mathbb{R}^{K \times K}$, residual vector $r \in \mathbb{R}^N$ and inverse length $c \in \mathbb{R}$, satisfying the following constraints:
\begin{align}
    A^\top L &= RB^\top + r(e_k)^\top \label{eq:bidiag_forward_constraint1}
    \\
    AR &= LB \label{eq:bidiag_forward_constraint2}
    \\
    Re_1 &= cv \label{eq:bidiag_forward_constraint3}
    \\
    I_{\leq} \circ [L^\top L] &= I \label{eq:bidiag_forward_constraint4}
    \\
    I_{\leq} \circ [R^\top R] &= I \label{eq:bidiag_forward_constraint5}
    \\
    R^\top r &= 0 \label{eq:bidiag_forward_constraint6}
\end{align}
and $B$ is diagonally banded as follows:
\begin{align}
B = \underbrace{\text{diag}(\boldsymbol{\alpha}) + \text{diag}(\boldsymbol{\beta}, \text{k=1})}_{\text{B is 'upper bidiagonal'}} = \begin{bmatrix}
\alpha_1  & \beta_1 & 0 & 0 & 0 & 0 \\
0 & \alpha_2 & \beta_2 & 0 & 0 & 0\\
0 & 0 & \ddots & \ddots & 0 & 0\\
0 & 0 & 0 & 0 & \alpha_{K-1}  & \beta_{K-1}\\
0 & 0 & 0 & 0 & 0 & \alpha_K\\
\end{bmatrix} \in \mathbb{R}^{K \times K} \label{eq:bidiag_forward_constraint7}
\end{align}


\subsection{Hessenberg Factorization}
$$\texttt{Hessenberg}_K: (A, v) \mapsto (Q, H, r, c)$$
Given a square matrix $A \in \mathbb{R}^{N \times N}$ an initial vector $v \in \mathbb{R}^N$ and a prescribed number of iterations $K \in \mathbb{N}_+$, the Hessenberg factorization produces a column-orthogonal $Q \in \mathbb{R}^{N \times K}$, 
structured $H \in \mathbb{R}^{K \times K}$, residual vector $r \in \mathbb{R}^N$, 
and reciprocal length $c \in \mathbb{R}$ such that
\begin{align}
AQ &= QH + r(e_K)^\top \label{eq:hessenberg_forward_constraint1}
\\
Qe_1 &= cv \label{eq:hessenberg_forward_constraint2}
\\
I_{\leq} \circ [Q^\top Q] &= I \label{eq:hessenberg_forward_constraint3}
\\
I_{\ll} \circ H &= 0 \label{eq:hessenberg_forward_constraint4}
\\
Q^\top r &= 0 \label{eq:hessenberg_forward_constraint5}
\end{align}


\subsection{Hessenberg factorization generalizes Bidiagonalization}\label{sec:bidiag_through_hessenberg}
To compute $\texttt{Bidiagonalize}_K(A, v) = (L, R, B, r, c)$ for any $A \in \mathbb{R}^{N \times M}$ and $v \in \mathbb{R}^M$ using Hessenberg factorization, we construct the symmetric matrix $\dddot{A}$ and augmented vector $\dddot{v}$:
\begin{align}
    \dddot{A} := \begin{bmatrix}\mathbf{0} & A \\ A^\top & \mathbf{0}\end{bmatrix} \in \mathbb{R}^{(N+M) \times (N+M)} \label{eq:A_sparsity_pattern}
\end{align}
    \begin{align}
\dddot{v} := \begin{pmatrix}\mathbf{0} \\ v \end{pmatrix} \in \mathbb{R}^{N+M} \label{eq:v_sparsity_pattern}
\end{align}
We define the operation $\texttt{augment}: (A, v) \mapsto (\dddot{A}, \dddot{v})$ as the augmentation of a matrix and vector to this symmetric matrix and padded vector.
Applying Hessenberg factorization for $2K$ iterations to this augmented input yields
$\texttt{Hessenberg}_{2K}(\dddot{A}, \dddot{v}) = (\dddot{Q}, \dddot{H}, \dddot{r}, c)$
which are directly related to $(L, R, B, r, c)$ through the following sparse equations:

\begin{align}
\dddot{Q} :&= \begin{bmatrix}\mathbf{0} & l_1 & \mathbf{0} & l_2 & \cdots & \mathbf{0} & l_k \\ r_1 & \mathbf{0} & r_2 & \mathbf{0} &  \cdots & r_k & \mathbf{0} \end{bmatrix} \in \mathbb{R}^{(N+M) \times 2K}  \label{eq:Q_sparsity_pattern} 
\end{align}
\begin{align}
\dddot{r} &:=\begin{pmatrix}\mathbf{0} \\ r \end{pmatrix} \in \mathbb{R}^{N+M} \label{eq:r_sparsity_pattern}
\end{align}
\begin{align}
\dddot{H} := \begin{bmatrix}
0 & \alpha_1 & 0  & 0 & 0 & 0 & 0 & 0\\
\alpha_1 & 0  & \beta_1 & 0 & 0 & 0 & 0 & 0\\
0 & \beta_1  & 0 & \alpha_2 & 0 & 0 & 0 & 0\\
0 & 0 & \alpha_2 & 0  & \beta_2 & 0 & 0 & 0\\
0 & 0 & 0 & \beta_2  & 0 & \ddots & 0 & 0\\
0 & 0 & 0 & 0   & \ddots & 0 & \beta_{K-1} & 0\\
0 & 0 & 0 & 0 & 0 & \beta_{K-1}  & 0 & \alpha_K\\
0 & 0 & 0 & 0 & 0 & 0  & \alpha_K & 0\\
\end{bmatrix} \in \mathbb{R}^{2K \times 2K} \label{eq:H_sparsity_pattern}
\end{align}
The vectors $l_i$ and $r_i$ in $\dddot{Q}$ are exactly the columns of $L$ and $R$, while the bands in $\dddot{H}$ hold the entries of the bidiagonal matrix $B$ with duplicated $\alpha_i$ and $\beta_i$ values. The residual vector $\dddot{r}$ is simply a padded version of $r$, and the inverse norm $c$ is unchanged.
\\
We formalize this relationship through a simple linear extraction mapping 
$$\texttt{extract}: (\dddot{Q}, \dddot{H}, \dddot{r}, c) \mapsto (L, R, B, r, c)$$ 
that projects away the zeros and extracts the relevant non-zero entries from the Hessenberg outputs.\footnote{The projection from $\dddot{H}$ to $B$ is not unique, as $\dddot{H}$ contains two copies of each $\alpha_i$ and $\beta_i$. We arbitrarily select the superdiagonal entries and drop the subdiagonal entries. This choice affects the sparsity pattern of $\nabla_{\dddot{H}} \rho$ in section \ref{subsec:derivation_stable_adjoint_system}. Any other convex linear combination of each pair would also be valid.} This allows us to theoretically define \textbf{augmented Bidiagonalization} as $$\overline{\texttt{Bidiagonalize}}_K := \texttt{extract} \circ \texttt{Hessenberg}_{2K} \circ \texttt{augment} : (A, v) \mapsto (L, R, B, r, c)$$ which is functionally equivalent to standard Bidiagonalization but inherits the numerical stability properties of Hessenberg factorization's adjoint system. 
\\
The problem is that this is "computing a lot of zeros". We will show that we can prune the computational graph to minimize work.

\section{Adjoint systems}

\subsection{Adjoint system of Hessenberg Factorization}\label{subsec:adjoint_hessenberg}
Let $K \in \mathbb{N}_+$, $A \in \mathbb{R}^{N\times N}$, $v \in \mathbb{R}^N$, $H \in \mathbb{R}^{K\times K}$, $Q \in \mathbb{R}^{N\times K}$, $r \in \mathbb{R}^N$, $c \in \mathbb{R}$ satisfy the primal constraints \eqref{eq:hessenberg_forward_constraint1} through \eqref{eq:hessenberg_forward_constraint5}. Let adjoint variables $\Lambda \in \mathbb{R}^{N\times K}$ with $\Lambda e_i = \lambda_i$, $\Gamma \in \mathbb{R}^{K\times K}$, $\lambda \in \mathbb{R}^N$, $\gamma \in \mathbb{R}^K$ solve the following system of equations:
\begin{align}
0 &= \nabla_{Q} \rho + A^\top \Lambda - \Lambda H^\top + \lambda (e_{1})^\top 
   + Q(I_{\leq} \circ \Gamma) + Q(I_{\leq} \circ \Gamma)^\top + r \gamma^\top \label{eq:adjoint_hessenberg_constraint1}\\ 
0 &= \nabla_{H} \rho - Q^\top \Lambda + I_{\ll} \circ \Sigma \label{eq:adjoint_hessenberg_constraint2}\\
0 &= \nabla_{r} \rho - \Lambda e_{K} + Q \gamma \label{eq:adjoint_hessenberg_constraint3}\\
0 &= \nabla_{c} \rho - v^\top \lambda \label{eq:adjoint_hessenberg_constraint4}
\end{align}
The gradients $\nabla_A\rho$ and $\nabla_v\rho$ are then given by\footnote{note a sign difference between \eqref{eq:adjoint_hessenberg_constraint6} and the result in TODO CITE due to a typo}:
\begin{align}
    \nabla_A\rho &= \Lambda Q^\top \label{eq:adjoint_hessenberg_constraint5} \\
    \nabla_v\rho &= -c\lambda \label{eq:adjoint_hessenberg_constraint6}
\end{align}


\subsection{Solving the Adjoint System}\label{subsec:solving_adjoint_system}
The output gradients $\nabla_Q\rho$, $\nabla_H\rho$, $\nabla_r\rho$, $\nabla_c\rho$ will have know quantities, so we need only determine the adjoint variables $\Lambda$, $\Gamma$, $\lambda$, $\gamma$. $\Gamma$ is involved only in equation \eqref{eq:adjoint_hessenberg_constraint1} in which only the lower triangular part is accessed, so for notational convenience we will redefine $\Gamma$ to be lower triangular, meaning we can effectively drop the mask $\Gamma := I_{\leq} \circ \Gamma$.
\\
We get expressions for $\lambda_K$ and $\gamma$:
\begin{align}
    \eqref{eq:adjoint_hessenberg_constraint3} & \iff & \lambda_K &= \nabla_r \rho + Q \gamma \label{eq:get_last_lambda}
    \\
    & \;\overset{\eqref{eq:hessenberg_forward_constraint3}}{\implies} &Q^\top \lambda_K &= Q^\top \nabla_r \rho + Q^\top Q \gamma \nonumber
    \\ 
    &&&= Q^\top \nabla_r \rho + \gamma \nonumber
    \\ 
    & \iff & \gamma &= Q^\top \lambda_K - Q^\top \nabla_r \rho \label{eq:get_gamma_almost}
\end{align}
To evaluate $Q^\top \lambda_k$, inspect \eqref{eq:adjoint_hessenberg_constraint2}:
\begin{align}
    \eqref{eq:adjoint_hessenberg_constraint2} & \iff &Q^\top \Lambda &= \nabla_{H} \rho + I_{\ll} \circ \Sigma \nonumber
    \\
    & \;\implies & (\mathbf{1} - I_{\ll}) \circ Q^\top \Lambda &= (\mathbf{1} - I_{\ll}) \circ \nabla_{H} \rho + (\mathbf{1} - I_{\ll}) \circ I_{\ll} \circ \Sigma \nonumber
    \\ 
    &&&= (\mathbf{1} - I_{\ll}) \circ \nabla_{H} \rho \label{eq:get_lambda_in_Q_coords}
    \\
    & \;\implies & Q^\top \lambda_K &= \nabla_{H} \rho e_K \nonumber
    \\
    &\;\overset{\eqref{eq:get_gamma_almost}}{\implies} &  \gamma &= \nabla_{H} \rho e_K - Q^\top \nabla_r \rho \label{eq:get_gamma}
\end{align}
This final relation lets us express $\gamma$ in terms of the known quantities, which we can then plug into \eqref{eq:get_last_lambda} to get $\lambda_K$.
\\
Next, we will get expressions for the remaining columns of $\Lambda$ and $\Gamma$, as well as for $\lambda$. As a preliminary, from primal constraints \eqref{eq:hessenberg_forward_constraint2} we have $v = q_1/c$, yielding 
\begin{align}
    \eqref{eq:adjoint_hessenberg_constraint4} \iff c\nabla_{c} \rho = q_1^\top \lambda\label{eq:get_q1_top_lambda}
\end{align}
We now turn to adjoint system constraint \eqref{eq:adjoint_hessenberg_constraint1}, left-multiply $Q^\top$ and then apply a mask to consider only the upper triangular part of the equations:
\begin{align*}
    \eqref{eq:adjoint_hessenberg_constraint1} &\overset{\eqref{eq:hessenberg_forward_constraint3}, \eqref{eq:hessenberg_forward_constraint5}}{\implies} \empty & \mathbf{0} &= Q^\top \nabla_{Q} \rho + Q^\top A^\top \Lambda - Q^\top \Lambda H^\top + Q^\top \lambda (e_{1})^\top + \Gamma + \Gamma^\top
    \\
    \empty &\;\implies\; \empty & \mathbf{0} &= I_\geq \circ \bigg[Q^\top \nabla_{Q} \rho + Q^\top A^\top \Lambda - Q^\top \Lambda H^\top + Q^\top \lambda (e_{1})^\top + \Gamma + \Gamma^\top\bigg]
    \\
    \empty & \empty & &= I_\geq \circ \bigg[Q^\top \nabla_{Q} \rho + Q^\top A^\top \Lambda - Q^\top \Lambda H^\top + Q^\top \lambda (e_{1})^\top\bigg] + \text{diag}(\Gamma) + \Gamma^\top
\end{align*}
We now inspect column $i$ in this matrix equation. We will use sparsity of $H$ to rewrite the matrix product $Q^\top \Lambda H^\top$, and we will apply some simplifications with the mask $I_\geq$:
\begin{align}
    \mathbf{0} &= \left(I_\geq \circ \bigg[Q^\top \nabla_{Q} \rho + Q^\top A^\top \Lambda - Q^\top \Lambda H^\top + Q^\top \lambda (e_{1})^\top\bigg]\right)e_i + \left(\text{diag}(\Gamma) + \Gamma^\top\right) e_i \nonumber
    \\
    &= I_\geq e_i \circ \bigg[Q^\top \nabla_{q_i} \rho + Q^\top A^\top \lambda_i - Q^\top \Lambda H^\top e_i + Q^\top \lambda (e_{1})^\top e_i\bigg] + \text{diag}(\Gamma)e_i + \Gamma^\top e_i \nonumber
    \\
    &= I_\geq e_i \circ \bigg[Q^\top \nabla_{q_i} \rho + Q^\top A^\top \lambda_i - Q^\top \left(\sum_{j=i-1}^{K} \lambda_j h_{ij}\right)\bigg] +(I_\geq e_i) \circ Q^\top \lambda \delta_{i=1} + \Gamma_{ii}e_i + \Gamma^\top e_i \nonumber
    \\
    &= I_\geq e_i \circ \bigg[Q^\top \nabla_{q_i} \rho + Q^\top A^\top \lambda_i - \sum_{j=i-1}^{K} h_{ij}Q^\top\lambda_j\bigg] + e_1q_1^\top \lambda \delta_{i=1} + \Gamma_{ii}e_i + \Gamma^\top e_i \nonumber
    \\
    &\!\overset{\eqref{eq:get_q1_top_lambda}}{=} I_\geq e_i \circ \bigg[Q^\top \nabla_{q_i} \rho + Q^\top A^\top \lambda_i\bigg] - I_\geq e_i \circ \left(\sum_{j=i-1}^{K} h_{ij}Q^\top\lambda_j\right) + e_1c\nabla_{c} \rho \delta_{i=1} + \Gamma_{ii}e_i + \Gamma^\top e_i \nonumber
    \\
    &= I_\geq e_i \circ \bigg[Q^\top \nabla_{q_i} \rho + Q^\top A^\top \lambda_i\bigg] - \left(\sum_{j=i-1}^{K} h_{ij} I_\geq e_i \circ Q^\top\lambda_j\right) + e_1c\nabla_{c} \rho \delta_{i=1} + \Gamma_{ii}e_i + \Gamma^\top e_i \label{eq:term_round_parentheses}
\end{align}
We can use \eqref{eq:get_lambda_in_Q_coords} to rewrite the term in round parentheses:
\begin{align}
    &\eqref{eq:get_lambda_in_Q_coords}:& (\mathbf{1} - I_{\ll}) \circ Q^\top \Lambda &= (\mathbf{1} - I_{\ll}) \circ \nabla_{H} \rho \nonumber &
    \\
    &\;\implies &  (\mathbf{1} - I_{<}) \circ Q^\top \Lambda &= (\mathbf{1} - I_{<}) \circ \nabla_{H} \rho \nonumber &
    \\
    &\iff &  I_{\geq} \circ Q^\top \Lambda &= I_{\geq} \circ \nabla_{H} \rho \nonumber &
    \\
    &\;\implies &  I_{\geq}e_i \circ Q^\top \lambda_i &= I_{\geq}e_i \circ \nabla_{H} \rho e_i \nonumber &
\end{align}
We plug this into \eqref{eq:term_round_parentheses}, which then yields an expression we can use to solve for a column $i$ of $\Gamma^\top$, requiring knowledge only of the value of $\lambda_i$:
\begin{align}
    \Gamma_{ii}e_i + \Gamma^\top e_i = - I_\geq e_i \circ \bigg[Q^\top \nabla_{q_i} \rho + Q^\top A^\top \lambda_i\bigg] + \left(\sum_{j=i-1}^{K} h_{ij} I_\geq e_i \circ \nabla_{H} \rho e_j\right) - e_1c\nabla_{c} \rho \delta_{i=1} \label{eq:get_Gamma_i}
\end{align}
\\
Finally, to solve for $\lambda_i$ and $\lambda$, we look again at \eqref{eq:adjoint_hessenberg_constraint1} and restrict to column $i$:
\begin{align}
    \eqref{eq:adjoint_hessenberg_constraint1} \implies & \empty & \!\!0 &\!= \!\nabla_{\!q_i} \rho + A^\top \!\lambda_i - \!\Lambda H^{\!\top}\!e_i + \lambda \delta_{i=1} 
    + Q\Gamma e_i + Q\Gamma^\top e_i + r \gamma^\top e_i \nonumber
    \\
    &\empty &\empty &\!= \!\nabla_{q_i} \rho + A^\top \lambda_i - \!\sum_{j=i-1}^{K} h_{ij} \lambda_j + \lambda \delta_{i=1} + Q(\Gamma + \Gamma^\top)e_i + r \gamma^\top e_i \nonumber
    \\
    \iff&  & \!\!\!\!\!\begin{array}{cc}
        - \lambda & \!\!i = 1 \\
      h_{i, i\!-\!1} \lambda_{i\!-\!1} & \!\!i > 1
   \end{array}\!\!\bigg\} &\!= \!\nabla_{q_i} \rho + A^\top \lambda_i - \!\sum_{j=i}^{K} h_{ij} \lambda_j + Q(\Gamma + \Gamma^\top)e_i + r \gamma^\top e_i \label{eq:get_lambda_i}
\end{align}
We now have all equations necessary to solve for all adjoint variables. There is a part that we have not yet addressed: In \eqref{eq:get_lambda_i}, we will need access to $(\Gamma + \Gamma^\top)e_i$, however \eqref{eq:get_Gamma_i} will only give us $\Gamma^\top e_i$. How we do this is best explained with an example.
\newtheorem{example}{Example}
\begin{example}[Construction of $\Gamma + \Gamma^\top$]\label{ex:construction_Gamma_Gamma_top}
Let $K=4$. For decreasing $i$ starting from $K$ down to $1$, the known entries in upper triangular $\Gamma^\top \in \mathbb{R}^{4 \times 4}$ will evolve as follows, where $?$ denotes unknown entries and $*$ denotes known entries:
\begin{align*}
    \;\;\;\;\;\;\Gamma^\top:
\begin{array}{c} i = 4 \\
    \begin{bmatrix}
    ? & ? & ? & * \\
    0 & ? & ? & * \\
    0 & 0 & ? & * \\
    0 & 0 & 0 & * \\
\end{bmatrix} \end{array} \qquad
\begin{array}{c} i = 3  \\
\begin{bmatrix}
    ? & ? & * & * \\
    0 & ? & * & * \\
    0 & 0 & * & * \\
    0 & 0 & 0 & * \\
\end{bmatrix} \end{array} \qquad
\begin{array}{c} i = 2 \\
\begin{bmatrix}
    ? & * & * & * \\
    0 & * & * & * \\
    0 & 0 & * & * \\
    0 & 0 & 0 & * \\
\end{bmatrix} \end{array} \qquad
\begin{array}{c} i = 1 \\
\begin{bmatrix}
    * & * & * & * \\
    0 & * & * & * \\
    0 & 0 & * & * \\
    0 & 0 & 0 & * \\
\end{bmatrix} \end{array} \qquad
\end{align*}
This is enough information to then also determine the $i$th column of symmetric $\Gamma + \Gamma^\top$, whose entries we will uncover in the following sequence:
\begin{align*}
    \Gamma + \Gamma^\top:
\begin{array}{c} i = 4 \\
    \begin{bmatrix}
    ? & ? & ? & * \\
    ? & ? & ? & * \\
    ? & ? & ? & * \\
    * & * & * & * \\
\end{bmatrix} \end{array} \qquad
\begin{array}{c} i = 3  \\
\begin{bmatrix}
    ? & ? & * & * \\
    ? & ? & * & * \\
    * & * & * & * \\
    * & * & * & * \\
\end{bmatrix} \end{array} \qquad
\begin{array}{c} i = 2 \\
\begin{bmatrix}
    ? & * & * & * \\
    * & * & * & * \\
    * & * & * & * \\
    * & * & * & * \\
\end{bmatrix} \end{array} \qquad
\begin{array}{c} i = 1 \\
\begin{bmatrix}
    * & * & * & * \\
    * & * & * & * \\
    * & * & * & * \\
    * & * & * & * \\
\end{bmatrix} \end{array} \qquad
\end{align*}
\end{example}
We now give the order in which to apply all the equations: 
\\
Initially, use \eqref{eq:get_gamma} to get $\gamma$, then follow that up with \eqref{eq:get_last_lambda} to get $\lambda_K$. 
\\Loop for $i = K$ down to $1$:
\\
Use \eqref{eq:get_Gamma_i} to get $\Gamma^\top e_i$ to then get $(\Gamma + \Gamma^\top)e_i$. Use in \eqref{eq:get_lambda_i} to solve for $\lambda_{i-1}$, or, if $i=1$, $\lambda$.
\\ Finally, we know the adjoint variables and can compute the gradients in \eqref{eq:adjoint_hessenberg_constraint5} and \eqref{eq:adjoint_hessenberg_constraint6}.

\subsection{Derivation of the Stable Adjoint System} \label{subsec:derivation_stable_adjoint_system}
As described in \ref{sec:bidiag_through_hessenberg}, we can use $\overline{\texttt{Bidiagonalize}}_K$ to (inefficiently) bidiagonalize any matrix $A$ with initial vector $v$. The detour through $\texttt{Hessenberg}$ let's us use the adjoint system of Hessenberg Factorization described in \ref{subsec:adjoint_hessenberg}. In this section we will inspect the adjoint system of $\overline{\texttt{Bidiagonalize}}_K$, and simplify it considerably by exploiting sparsity in $\dddot{A}, \dddot{v}, \dddot{Q}, \dddot{H}$ and $\dddot{r}$.
\\
The sparsity patterns of $\nabla_{\dddot{Q}} \rho$, $\nabla_{\dddot{H}} \rho$, and $\nabla_{\dddot{r}} \rho$ match those of $\dddot{Q}, \dddot{H}, \dddot{r}$, due to the linear nature of \texttt{extract}:
\begin{align}
    \nabla_{\dddot{Q}} \rho := \begin{bmatrix}\mathbf{0} & \nabla_{l_1}\rho & \mathbf{0} & \nabla_{l_2}\rho & \cdots & \mathbf{0} & \nabla_{l_k}\rho \\ \nabla_{r_1}\rho & \mathbf{0} & \nabla_{r_2}\rho & \mathbf{0} &  \cdots & \nabla_{r_k}\rho & \mathbf{0} \end{bmatrix} \in \mathbb{R}^{(N+M) \times K} \label{eq:nabla_Q_sparsity_pattern}
\end{align}
\begin{align}
    \nabla_{\dddot{r}} \rho := \begin{pmatrix}\mathbf{0} \\ \nabla_{r} \rho \end{pmatrix} \in \mathbb{R}^{N+M} \label{eq:nabla_r_sparsity_pattern}
\end{align}
\begin{align}
    \nabla_{\dddot{H}} \rho := \begin{bmatrix} 
    0 & \nabla_{\alpha_1}\rho & 0  & 0 & 0 & 0 & 0 & 0\\
    0 & 0  & \nabla_{\beta_1}\rho & 0 & 0 & 0 & 0 & 0\\
    0 & 0  & 0 & \nabla_{\alpha_2}\rho & 0 & 0 & 0 & 0\\
    0 & 0 & 0 & 0 & \nabla_{\beta_2}\rho & 0 & 0 & 0\\
    0 & 0 & 0 & 0 & 0 & \ddots & 0 & 0\\
    0 & 0 & 0 & 0 & 0 & 0 & \nabla_{\beta_{K-1}}\rho & 0\\
    0 & 0 & 0 & 0 & 0 & 0  & 0 & \nabla_{\alpha_K}\rho\\
    0 & 0 & 0 & 0 & 0 & 0  & 0 & 0\\
    \end{bmatrix} \in \mathbb{R}^{2K \times 2K} \label{eq:nabla_H_sparsity_pattern}
\end{align}
We will look at the constraints \eqref{eq:adjoint_hessenberg_constraint1} through \eqref{eq:adjoint_hessenberg_constraint4} and determine further sparsity patterns in the adjoint variables $\Lambda, \Gamma, \lambda, \gamma$. We will mark possibly nonzero entries with $*$, and constant zero entries with a $0$.
\\
\subsubsection{Inspecting $\gamma$ and $\lambda_{2K}$}
Starting from \eqref{eq:get_gamma}, replacing $K$ with $2K$, we plug in our sparse gradients and outputs, and show that, for this input, $\gamma \in \mathbb{R}^{2K}$ is alternatingly non-zero and zero.
\begin{align}
    \eqref{eq:get_gamma} \implies \gamma &= \nabla_{\dddot{H}} \rho e_{2K} - \dddot{Q}^\top \nabla_{\dddot{r}} \rho = \nabla_{\alpha_{K}} \rho e_{2K-1} - \dddot{Q}^\top \begin{pmatrix}\mathbf{0} \\ \nabla_r\rho\end{pmatrix} 
    \\
    &= \begin{pmatrix}0 \\ 0 \\ 0 \\ 0 \\ \vdots \\ 0 \\ \nabla_{\alpha_K} \rho \\ 0\end{pmatrix}  - \begin{pmatrix}r_1^\top \\ \mathbf{0}^\top \\ r_2^\top \\ \mathbf{0}^\top \\ \vdots \\\mathbf{0}^\top\\ r_K^\top \\ \mathbf{0}^\top\end{pmatrix} \nabla_{r} \rho \equiv \begin{pmatrix}* \\ 0 \\ * \\ 0 \\ \vdots \\ 0 \\ * \\ 0\end{pmatrix}
\end{align}
We plug this pattern into \eqref{eq:get_last_lambda}, which shows that $\lambda_{2K}$ has the same sparsity pattern as $\dddot{v}$:
\begin{align}
    \eqref{eq:get_last_lambda} \implies \lambda_{2K} &= \nabla_{\dddot{r}} \rho + \dddot{Q} \gamma
    \equiv \begin{pmatrix}\mathbf{0} \\ \nabla_r\rho\end{pmatrix}  + \dddot{Q} \begin{pmatrix}* \\ 0 \\ * \\ 0 \\ \vdots \\ 0\\ * \\ 0\end{pmatrix}
    \equiv \begin{pmatrix}0 \\ 0 \\ \vdots \\ 0 \\ * \\ * \\ \vdots \\ *\end{pmatrix} + \begin{pmatrix}0 \\ 0 \\ \vdots \\ 0 \\ * \\ * \\ \vdots \\ *\end{pmatrix}
    \equiv \begin{pmatrix}0 \\ 0 \\ \vdots \\ 0 \\ * \\ * \\ \vdots \\ *\end{pmatrix} \label{eq:lambda_2K_sparsity_pattern}
\end{align}
\subsubsection{Inspecting $\Gamma^\top$ and $\Lambda$}
We move onto equation \eqref{eq:get_Gamma_i} for columns of $\Gamma^\top$:
\begin{align}
    \Gamma_{ii}e_i + \Gamma^\top e_i = - I_\geq e_i \circ \bigg[\dddot{Q}^\top \nabla_{\dddot{q_i}} \rho + \dddot{Q}^\top \dddot{A}^\top \lambda_i\bigg] + \left(\sum_{j=i-1}^{K} h_{ij} I_\geq e_i \circ \nabla_{\dddot{H}} \rho e_j\right) - e_1c\nabla_{c} \rho \delta_{i=1}
\end{align}
By expanding the sparsity patterns of known variables $\dddot{Q}$, $\dddot{A}$, $\nabla_{\dddot{q_i}} \rho$, and $\nabla_{\dddot{H}} \rho$, this will simplify. Expanding one term at a time starting with $\dddot{Q}^\top \nabla_{\dddot{q_{i}}} \rho$ using \eqref{eq:Q_sparsity_pattern} and \eqref{eq:nabla_Q_sparsity_pattern}, we get, for even $i$:
\begin{align}
    \dddot{Q}^\top \nabla_{\dddot{q_{i}}} \rho = 
    \begin{bmatrix}\mathbf{0} & l_1 & \mathbf{0} & l_2 & \cdots & \mathbf{0} & l_k \\ r_1 & \mathbf{0} & r_2 & \mathbf{0} &  \cdots & r_k & \mathbf{0} \end{bmatrix} ^\top \begin{bmatrix} \nabla_{l_i} \rho \\ \mathbf{0} \end{bmatrix} = 
    \begin{bmatrix} l_1^\top \nabla_{l_i} \rho & 0 & l_2^\top \nabla_{l_i} \rho & 0 & \cdots & l_k^\top \nabla_{l_i} \rho & 0\end{bmatrix} ^\top
\end{align}
For odd $i$, we get 
\begin{align}
    \dddot{Q}^\top \nabla_{\dddot{q_{i}}} \rho = 
    \begin{bmatrix}\mathbf{0} & l_1 & \mathbf{0} & l_2 & \cdots & \mathbf{0} & l_k \\ r_1 & \mathbf{0} & r_2 & \mathbf{0} &  \cdots & r_k & \mathbf{0} \end{bmatrix} ^\top \begin{bmatrix} \mathbf{0} \\ \nabla_{r_i} \rho \end{bmatrix} =
    \begin{bmatrix} 0 & r_1^\top \nabla_{r_i} \rho & 0 & r_2^\top \nabla_{r_i} \rho  & \cdots & 0 & r_k^\top \nabla_{r_i} \rho \end{bmatrix} ^\top
\end{align}
Next term is $\dddot{Q}^\top \dddot{A}^\top \lambda_i$ using again \eqref{eq:Q_sparsity_pattern} and \eqref{eq:A_sparsity_pattern}. Assume that $\Lambda$ has the opposite sparsity pattern as $\dddot{Q}$. This lets us name the nonzero entries: 
\begin{align}
    \Lambda = \begin{bmatrix}\varphi_1 & \mathbf{0} & \varphi_2 & \mathbf{0} & \cdots &\varphi_k & \mathbf{0}\\ \mathbf{0} & \xi_1 & \mathbf{0} & \xi_2 &  \cdots  & \mathbf{0} & \xi_k \end{bmatrix} \in \mathbb{R}^{(N+M) \times 2K} \label{eq:Lambda_sparsity_pattern}
\end{align}
The proof of \eqref{eq:Lambda_sparsity_pattern} will follow from induction backwards from $i=2K$ to $i=1$, with the base case already shown in \eqref{eq:lambda_2K_sparsity_pattern}. First, for even $i$, we get:
\begin{align}
    \dddot{Q}^\top \dddot{A}^\top \lambda_i = 
    \begin{bmatrix}\mathbf{0} & l_1 & \mathbf{0} & l_2 & \cdots & \mathbf{0} & l_k \\ r_1 & \mathbf{0} & r_2 & \mathbf{0} &  \cdots & r_k & \mathbf{0} \end{bmatrix} ^\top \begin{bmatrix} \mathbf{0} & A \\ A^\top & \mathbf{0} \end{bmatrix} \begin{bmatrix} \mathbf{0} \\ \xi_i \end{bmatrix} = 
    \begin{bmatrix} 0 & l_1^\top A\xi_i & 0 & l_2^\top A\xi_i  & \cdots & 0 & l_k^\top A\xi_i \end{bmatrix} ^\top
\end{align}
For odd $i$, we get:
\begin{align}
    \dddot{Q}^\top \dddot{A}^\top \lambda_i = 
    \begin{bmatrix}\mathbf{0} & l_1 & \mathbf{0} & l_2 & \cdots & \mathbf{0} & l_k \\ r_1 & \mathbf{0} & r_2 & \mathbf{0} &  \cdots & r_k & \mathbf{0} \end{bmatrix} ^\top \begin{bmatrix} \mathbf{0} & A \\ A^\top & \mathbf{0} \end{bmatrix} \begin{bmatrix} \varphi_i \\ \mathbf{0} \end{bmatrix} = 
    \begin{bmatrix} r_1^\top A\varphi_i & 0 & r_2^\top A\varphi_i  & 0 & \cdots & r_k^\top A\varphi_i & 0 \end{bmatrix} ^\top
\end{align}
Next term to dissect is $\sum_{j=i-1}^{K} h_{ij} I_\geq e_i \circ \nabla_{\dddot{H}} \rho e_j$. Here, we use the sparsity pattern of $\nabla_{\dddot{H}} \rho$ from \eqref{eq:nabla_H_sparsity_pattern}:
\begin{align}
    \text{for even $i$: }& &\nabla_{\dddot{H}} \rho e_i &= \nabla_{\alpha_{i/2}} \rho e_{i-1}
    \\
\text{for odd $i$: } & & \nabla_{\dddot{H}} \rho e_i &= \nabla_{\beta_{(i-1)/2}} \rho e_{i-1}
\end{align}
For this reason, all terms $I_\geq e_i \circ \nabla_{\dddot{H}} \rho e_j$ for $j \notin \{i-1, i, i+2\}$ are zero. Additionally, we know from \eqref{eq:H_sparsity_pattern} that $h_{ii} = 0$ for all $i$.
Assuming now $i$ even, we get:
\begin{align}
    \sum_{j=i-1}^{K} h_{ij} I_\geq e_i \circ \nabla_{\dddot{H}} \rho e_j  
    &= 
    \sum_{j=i-1}^{i+1} h_{ij} I_\geq e_i \circ \nabla_{\dddot{H}} \rho e_j 
    \\ &= h_{i,i-1} \nabla_{\dddot{H}} \rho e_{i-1} + h_{i,i} \nabla_{\dddot{H}} \rho e_i + h_{i,i+1} \nabla_{\dddot{H}} \rho e_{i+1}
    \\ &= h_{i,i-1} \nabla_{\dddot{H}} \rho e_{i-1} + h_{i,i+1} \nabla_{\dddot{H}} \rho e_{i+1}
    \\ &= \alpha_{i/2} \nabla_{\dddot{H}} \rho e_{i-1} + \beta_{i/2} \nabla_{\dddot{H}} \rho e_{i+1}
    \\ &= \alpha_{i/2} \nabla_{\beta_{i/2 - 1}}\rho e_{i-2} + \beta_{i/2} \nabla_{\beta_{i/2}}\rho e_{i}
\end{align}
For odd $i$, with a similar argument we'd get:
\begin{align}
    \sum_{j=i-1}^{K} h_{ij} I_\geq e_i \circ \nabla_{\dddot{H}} \rho e_j  
    &= h_{i,i-1} \nabla_{\dddot{H}} \rho e_{i-1} + h_{i,i+1} \nabla_{\dddot{H}} \rho e_{i+1}
    \\ &= \beta_{(i-1)/2} \nabla_{\dddot{H}} \rho e_{i-1} + \alpha_{(i+1)/2} \nabla_{\dddot{H}} \rho e_{i+1}
    \\ &= \beta_{(i-1)/2} \nabla_{\alpha_{(i-1)/2 - 1}}\rho e_{i-2} + \alpha_{(i+1)/2} \nabla_{\alpha_{(i+1)/2}}\rho e_{i}
\end{align}

Bringing it all together back into \eqref{eq:get_Gamma_i}, for even $i$, we get:
TODO

\subsubsection{Step 3: Summary? (TODO), or is it unnecessary?}
We will now give new names to all nonzero entries in $\Lambda, \Gamma, \lambda, \gamma$, and then show a more compact expression for the stable adjoint system.
As shown, $\Gamma$ has a checkered pattern, which, here shown for $K=6$ we name as: 
$$\Gamma = \begin{bmatrix} 
    \omega_{11} & 0 & 0 & 0 & 0 & 0 \\
    0 & \sigma_{11} & 0 & 0 & 0 & 0 \\
    \omega_{21} & 0 & \omega_{22} & 0 & 0 & 0 \\
    0 & \sigma_{21} & 0 & \sigma_{22} & 0 & 0 \\
    \omega_{31} & 0 & \omega_{32} & 0 & \omega_{33} & 0 \\
    0 & \sigma_{31} & 0 & \sigma_{32} & 0 & \sigma_{33} \\
\end{bmatrix} \begin{array}{c}\text{ with }\\\text{ aliases }\end{array} \begin{array}{c}\Omega = \begin{bmatrix}
    \omega_{11} & 0 & 0\\
    \omega_{21} & \omega_{22} & 0\\
    \omega_{31} & \omega_{32} & \omega_{33}\\
\end{bmatrix} \\ \\ \Sigma = \begin{bmatrix}
    \sigma_{11} & 0 & 0 \\
    \sigma_{21} & \sigma_{22} & 0 \\
    \sigma_{31} & \sigma_{32} & \sigma_{33} \\
\end{bmatrix}\end{array}$$
and the entries of $\lambda$ and $\gamma$ as 
$$\lambda = \begin{bmatrix}
    \mathbf{0} \\ \xi_0
\end{bmatrix} \qquad \gamma = \begin{bmatrix}
    \gamma_1 & 0 & \gamma_2 & 0 & \cdots & 0 & \gamma_K & 0
\end{bmatrix}^\top$$

\subsection{Columnwise expanded unstable adjoint}
Define $b_n\lambda_{n+1} := \mathbf{0}$ and $b_0\rho_0:=\mathbf{0}$
\begin{align}
0 &= \nabla l_n + a_n\lambda_n + b_n\lambda_{n+1} - A\rho_n + l_n\sigma_n
\\
0 &= \nabla r_n - A^\top\lambda_n + b_{n-1}\rho_{n-1} + a_n\rho_n + r_n\omega_n + \delta_{i,1} \kappa
\\
\rho_k &= - \nabla res
\\
\nabla a_n + l_n^\top\lambda_n + r_n^\top\rho_n &= 0 \text{ for } i \in \{1 \cdots k\}
\\
\nabla b_n + l_n^\top \lambda_{n+1} + r_{n+1}^\top \rho_{n} &= 0 \text{ for } i \in \{1 \cdots k-1\}
\\
\nabla c - \tilde r^\top\kappa &= 0
\\
\nabla \tilde r &= -c\kappa 
\\
\nabla A &= \sum_{n=1}^k -\lambda_n r_n^\top -l_n\rho_n^\top
\end{align}

\subsection{Hessenberg factorization has stable adjoint because of reprojection}
Some of the constraints of the adjoint system in Hessenberg factorization allow for a direct correction of drift \footnote{cite equation 13b}.
Because Hessenberg factorization subsumes Bidiagonalization for this specific structured input, we can use their implementation of numerically stable gradients to differentiate bidiagonalization.

\subsection{Unstable Primal constraints}
Define $l_{0}b_{0} := 0$ and $res := r_{k+1} b_k $
\begin{align*}
    r_1 &= c\tilde r
    \\
    \alpha_n l_n &= Ar_n - \beta_{n-1}l_{n-1}
    \\
    \beta_n r_{n+1} &= A^\top l_n - \alpha_n r_n 
    \\
    \langle l_i, l_i \rangle &= 1 \text{ for } i \in {1 \dots k}
    \\
    \langle r_i, r_i \rangle &= 1 \text{ for } i \in {1 \dots k}
\end{align*}
Symbolically, we can\footnote{proof by induction. Add?} show that these constraints, if satisfied, imply $\langle l_i, l_j \rangle =\delta_{i,j}$ and $\langle r_i, r_j \rangle =\delta_{i,j}$. In practice however, we need to reorthonormalize the produced vectors during iteration.
\\
These "classic" forward constraints and the associated adjoint system:
\subsection{Unstable Adjoint system}
\footnote{See another subsection for the same things all expanded columnwise}
This system can be derived from the bidiagonalization forward constraints directly.
\begin{align}
\rho_k &= - \nabla res
\\
c \nabla c &= r_1^\top\kappa
\\
\nabla \tilde r &= -c\kappa 
\\
0 &= \nabla L - AP + \Lambda B^\top + L\Sigma
\\
0 &= \nabla R - A^\top\Lambda + PB + R\Omega + \delta_{i,1} \kappa
\\
\langle l_n, \lambda_n \rangle + \langle r_n, \rho_n\rangle &= \nabla a_n \text{ for } i \in \{1 \cdots k\}
\\
\langle l_n, \lambda_{n+1} \rangle + \langle r_{n+1}, \rho_{n}\rangle &= \nabla b_n \text{ for } i \in \{1 \cdots k-1\}
\\
\nabla A &= \sum_{n=1}^k -\lambda_n r_n^\top -l_n\rho_n^\top
\end{align}

\subsection{Stable Adjoint System}

\begin{align}
    \downarrow_k &= \nabla_{res} + R \gamma
    \\
    r_1^\top \kappa &= c\nabla_c
    \\
    0 &= \nabla_L + A\Downarrow - \Uparrow B^\top + L(\Sigma + \Sigma^\top)
    \\
    0 &= \nabla_R + A^\top\!\!\Uparrow - \Downarrow B + R(\Omega + \Omega^\top) + \kappa e_1^\top + res \gamma^\top
    \\
    \langle l_i, \uparrow_j \rangle &= \nabla \beta_{i-1} e_{i-1} \text{  for } i\leq j
    \\
    \langle r_i, \downarrow_j \rangle &= \nabla \alpha_{i} e_{i} \text{  for } i\leq j+1
    \\
    \nabla_v &= -\kappa c
    \\
    \nabla_A &= \sum_{i=1}^k l_i \downarrow_i^\top + \uparrow_i r_i^\top
\end{align}

\subsection{The Adjoint Method to compute gradients}
The adjoint method provides an elegant way to compute parameter gradients of losses that depend on outputs of parameterized numerical solvers.
\\
\\
Let $s : \theta \mapsto x$ be a numerical solver that yields a solution $x$ to some problem $\theta$, with $\text{dim}(\theta) = P$ and $\text{dim}(x) = X$. Assume also that we know an associated constraint function $c : x \mapsto \mathbb{R}^X$ that evaluates all the constraints of the problem, for which $c(s(\theta), \theta) = \mathbf{0}$ for all $\theta$. Additionally, require that $\frac{\partial c}{\partial \theta}$ is everywhere nonsingular. Assume also that we have a loss function $\ell : x \mapsto \mathbb{R}$ that depends on the solution $x$ of the problem. We are interested in $\nabla_\theta \ell(s(\theta))$, the gradient of $\ell$ with respect to the parameters $\theta$.
\begin{example}[Hessenberg Factorization]
    A fitting example would be $s = $ \texttt{Hessenberg}, which implies that $\theta = (A, v)$ and $x = (Q, H, r, c)$. The associated constraint function would be the concatenation of constraints \eqref{eq:hessenberg_forward_constraint1} through \eqref{eq:hessenberg_forward_constraint5}. We see that, for this example, $c(x, \theta) = \mathbf{0}$ for all solutions $(Q, H, r, c)$ if and only if they correctly Hessenberg-factorize from $\theta$ = $(A, v)$.
\end{example}
Because $c(s(\theta), \theta) = \mathbf{0}$ is constantly zero, the total derivative $\frac{d}{d\theta} c$ is also zero. With the chain rule, we have:

\begin{align}
    \mathbf{0} = \frac{d}{d\theta} c(s(\theta), \theta) = \frac{\partial c}{\partial s} \frac{\partial s}{\partial \theta} + \frac{\partial c}{\partial \theta}
    \implies \frac{\partial c}{\partial s} \frac{\partial s}{\partial \theta} = -\frac{\partial c}{\partial \theta} \label{eq:exploit_constraint}
\end{align}
The adjoint method will exploit this relationship. We apply the chain rule to compute the gradient $\nabla_\theta \ell(s(\theta))$:
\begin{align}
    \nabla_\theta \ell(s(\theta)) = \frac{\partial s}{\partial \theta}^\top\nabla_x \ell(s(\theta))  \label{eq:gradient_chain_rule}
\end{align}
We assume that $\nabla_x \ell(s(\theta))$ is easy to obtain. The challenge is computing $\frac{\partial s}{\partial \theta} \in \mathbb{R}^{X \times P}$, which can be expensive for high-dimensional $x$. Suppose we can find \textit{adjoint variables} $\boldsymbol{\lambda} \in \mathbb{R}^X$ solving the uniquely determined $X \times X$ linear system (unique by initial assumption on the constraint Jacobian)
\begin{align}
    \nabla_x \ell(s(\theta)) = \frac{\partial c}{\partial s}^\top \boldsymbol{\lambda}  \label{eq:adjoint_system}
\end{align}
Then, substituting this into \eqref{eq:gradient_chain_rule} yields:
\begin{align*}
    \nabla_\theta \ell(s(\theta)) = \frac{\partial s}{\partial \theta}^\top \frac{\partial c}{\partial s}^\top \boldsymbol{\lambda} = \left(\frac{\partial c}{\partial s} \frac{\partial s}{\partial \theta}\right)^\top \boldsymbol{\lambda} \overset{\eqref{eq:exploit_constraint}}{=} -\frac{\partial c}{\partial \theta}^\top \boldsymbol{\lambda}
\end{align*}
The adjoint method provides significant computational savings compared to the standard approach. To see this, consider the two approaches:

\textit{Forward approach:} Compute $\frac{\partial s}{\partial \theta} \in \mathbb{R}^{X \times P}$ directly by solving $P$ linear systems of size $X \times X$ (one for each parameter), then compute the gradient via \eqref{eq:gradient_chain_rule}. Total cost: $O(PX^3)$ operations.

\textit{Adjoint approach:} Solve one linear system \eqref{eq:adjoint_system} of size $X \times X$ to find $\boldsymbol{\lambda}$, then evaluate $-\frac{\partial c}{\partial \theta}^\top \boldsymbol{\lambda}$. Total cost: $O(X^3 + XP)$ operations.

When $P \ll X$ (few parameters, high-dimensional state), the adjoint method is dramatically more efficient: $O(X^3)$ versus $O(PX^3)$. This advantage becomes crucial in applications like neural network training or PDE-constrained optimization where $X$ can be very large.

\section{Numerical Stability and Experiments}
\subsection{Reprojection and Reorthonormalization}
Let $m$ be a mask vector with entries from $\{0, 1\}$. Let $L$ be an orthonormal matrix, and $q$ and $c$ be vectors. Consider a constraint of the form
$$m \circ (L^\top q) = m \circ c$$
Paraphrased: "Within the bounds of the mask $m$, we have that $q$ expressed in the basis of $L$ equals $c$".
If we have access to a numerical estimate $q_0$ of a solution $q$, we can get an improved estimate $q_1$ by \textit{reprojecting} it against $L$.
\begin{align*}
    q_0 &= q_0 - L (m \circ L^\top q_0) + L (m \circ L^\top q_0)
    \\
    &= q_0 - L (m \circ L^\top q_0) + L (m \circ c)
\end{align*}
The following motivates following update step, which can be repeated if necessary.
\begin{align*}
    q_1 &\leftarrow q_0 - L (m \circ L^\top q_0) + L (m \circ c)
\end{align*}
In the case of Hessenberg factorization, we have the constraint
$Q^\top q_i = e_i$ \eqref{eq:hessenberg_forward_constraint3} in which case we call the reprojection step followed by normalization \textit{reorthonormalization}.
\\
For Bidiagonalization, we have similar constraints $L^\top l_i = e_i$ \eqref{eq:bidiag_forward_constraint4} and $R^\top r_i = e_i$ \eqref{eq:bidiag_forward_constraint5}. Empirically, we find that two repetitions of the reprojection step are necessary to eliminate numerical errors. TODO show this on a Hilbert matrix?


\end{document}